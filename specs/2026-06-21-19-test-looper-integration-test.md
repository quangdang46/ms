# Spec: Looper Integration Test for `ms`

> Issue: [quangdang46/ms#19](https://github.com/quangdang46/ms/issues/19)  
> Date: 2026-06-21  
> Status: Draft  

---

## Problem

The `ms` (meta_skill) project needs a verified Looper integration test to confirm that the full Looper pipeline — issue detection, planning, spec creation, review, fix implementation, and PR lifecycle — works end-to-end on this repository. Without a passing integration test, regressions in the Looper toolchain could silently break automation workflows for `ms`.

## Goals

1. **Verify pipeline stages** — Confirm Looper can:
   - Pick up issues labeled `looper:plan`
   - Execute planner, reviewer, fixer, and worker agent loops
   - Generate a planning spec from an issue
   - Create a spec PR on the correct branch
   - Apply code changes (if any) and review them

2. **Measure round-trip time** — Establish a baseline end-to-end timing for the full lifecycle on `ms`.

3. **Produce audit trail** — The spec PR, its review comments, and any fix commits form a reproducible record of what Looper did and how it performed.

## Non-Goals

- Testing Looper against external repos (scoped to `quangdang46/ms` only).
- Testing edge cases like conflicting labels, missing permissions, or rate-limit errors.
- Performance benchmarking or stress-testing with parallel issues.
- Modifying any production behaviour of `ms` itself (this is a pure integration test).

## Approach

### Phase 1: Planning (this spec)

Create `specs/2026-06-21-19-test-looper-integration-test.md` on the `looper/planner/19-test-looper-integration-test` branch. The planner agent:
1. Reads the issue body to understand the test scenario.
2. Gathers repository context (branch, project structure, toolchain).
3. Writes this planning spec with clear problem, goals, approach, risks, and validation.
4. Commits the spec and pushes the branch.

### Phase 2: Review (upstream — Looper reviewer agent)

The reviewer agent will:
1. Checkout the spec branch.
2. Validate spec completeness against the Looper spec rubric (problem, goals, approach, risks, validation all present).
3. Leave inline review comments and approve or request changes.

### Phase 3: Fix / Implementation (if applicable)

Since the issue is deliberately a test, the "fix" may be:
- An empty or minimal code change that exercises the fixer/worker agents.
- A small improvement such as adding a CI workflow step or a documentation note that the test passed.

The exact scope will be determined by the reviewer. The implementation branch will be `looper/fixer/19-<descriptive-suffix>`.

### Phase 4: Merge & Verify

1. The reviewer approves the final PR.
2. The PR is merged into `main`.
3. A human (or automated watcher) confirms the pipeline completed successfully.

## Risks

| Risk | Likelihood | Impact | Mitigation |
|------|-----------|--------|------------|
| Looper cannot push to the repo (auth/permissions) | Low | High | Verify `GITHUB_TOKEN` scopes before starting. Fall back to manual PR creation. |
| Spec branch conflicts with concurrent work | Low | Medium | Branch from `main`, keep worktree isolated. |
| Spec fails validation rubric | Medium | Low | Planner will revise based on reviewer comments. |
| Looper agent loses context mid-pipeline | Low | High | All state is in git; re-run from last checkpoint. |
| Pipeline times out waiting for reviewer | Medium | Low | Looper has configurable timeouts; flag for human escalation if exceeded. |

## Validation

The test is considered **passed** when:

1. **Spec created** — `specs/2026-06-21-19-test-looper-integration-test.md` exists on the `looper/planner/19-test-looper-integration-test` branch with all required sections.
2. **Spec PR opened** — A pull request exists from the planner branch targeting `main`, with the spec committed.
3. **Review completed** — Looper reviewer agent posts at least one review comment or approval on the PR.
4. **Fix/implementation applied** (if applicable) — A follow-up branch/PR with code changes is created and reviewed.
5. **PR merged** — The spec PR (and any fix PR) is merged into `main`.
6. **Labels updated** — The `looper:plan` label is removed and (`looper:reviewed`, `looper:spec`, etc.) applied as appropriate.

### Rollback

If the test fails at any stage:
- If only the spec branch exists: delete the branch (`git push origin --delete looper/planner/19-test-looper-integration-test`).
- If a PR is open: close it without merging.
- If merged: revert the commit on `main` with `git revert`.

No production data is at risk — this is a test issue against a development toolchain.

---

*Generated by Looper planner agent for issue #19.*
